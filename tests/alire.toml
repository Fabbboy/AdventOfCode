name = "aoc_tests"
description = "Tests for the built-in Alire test runner"
version = "0.0.0-test"

[[depends-on]]
aoc = "*"

[[pins]]
aoc = { path = ".." }

[build-profiles]
aoc       = "validation"
aoc_tests = "validation"
